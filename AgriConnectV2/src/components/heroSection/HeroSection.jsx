import AppCarousel from "../AppCourosel/AppCourosel";

const HeroSection = () => {
    return (
        <div className="mt-4">
            <AppCarousel />
        </div>
    );
}

export default HeroSection;
